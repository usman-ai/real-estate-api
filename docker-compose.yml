services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aredc
      POSTGRES_PASSWORD: aredc
      POSTGRES_DB: aredc
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aredc -d aredc"]
      interval: 3s
      timeout: 3s
      retries: 20

  postgres_test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aredc
      POSTGRES_PASSWORD: aredc
      POSTGRES_DB: aredc_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aredc -d aredc_test"]
      interval: 3s
      timeout: 3s
      retries: 20

  app:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://aredc:aredc@postgres:5432/aredc?schema=public
      JWT_SECRET: change-me-in-real-life-please
      JWT_EXPIRES_IN: 1d
      SEED_USER_PASSWORD: Passw0rd!
    # Compose-level override: apply migrations, seed test users (idempotent via
    # upsert), then start the app. Seeding is intentionally out of the Dockerfile
    # CMD so production images don't auto-seed.
    command: sh -c "npx prisma migrate deploy && npx prisma db seed && node dist/src/main.js"
    ports:
      - "3000:3000"

volumes:
  postgres_data:
